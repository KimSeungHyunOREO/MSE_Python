nums = [1, 2, 3, 4, 5]


import numpy as np # numpy를 이용해서 평균을 구했다.
np.mean(nums) # numpy에서 mean 함수를 사용하면 평균이 나온다.

# -------------------------------

nums = [1, 2, 3, 4, 5]
average = sum(nums) / len(nums) # 평균은 총 합을 개수로 나눈 값이므로 
print(average)

# numpy를 사용하면 빠르게 구할 수 있다.

